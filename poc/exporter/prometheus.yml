global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "prom-forge-exporter"
    static_configs:
      - targets: ["host.docker.internal:8080"]
        labels:
          app: "prom-forge-exporter"
